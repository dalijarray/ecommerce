import { DetailsProductPipe } from './details-product.pipe';

describe('DetailsProductPipe', () => {
  it('create an instance', () => {
    const pipe = new DetailsProductPipe();
    expect(pipe).toBeTruthy();
  });
});
