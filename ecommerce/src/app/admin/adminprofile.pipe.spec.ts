import { AdminprofilePipe } from './adminprofile.pipe';

describe('AdminprofilePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminprofilePipe();
    expect(pipe).toBeTruthy();
  });
});
